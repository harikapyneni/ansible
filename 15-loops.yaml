- name: install packages
  hosts: mongodb
  become: yes
  tasks:
    - name: install packages
      ansible.builtin.yum:
        name: "{{item}}"
        state: installed
      loop:
      - postfix
      - cyrus
      - sasl